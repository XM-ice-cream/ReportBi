import {
  requestCenter
} from '@/libs/request'
/*打件资料信息补录 接口 */

// 提交打件资料
export const addReq = data => {
  return requestCenter({
    timeout: 0,
    baseUrl: "http://10.32.44.119:8024" || window.localStorage.getItem("reportip"),
    url: '/api/v1/REPAIR_REFDES_API/AutoGenerateData',
    method: 'post',
    data
  })
}